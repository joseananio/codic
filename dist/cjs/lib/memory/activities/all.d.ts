import { ActivityModel } from "../../codic/activity/constructor";
/**
 * Returns a list of all activities
 * @returns Promise<Array<ActivityModel>>
 */
export default function all(): Promise<Array<ActivityModel>>;
